{{!-- {{#each dailys as |daily|}}
    {{> add-daily-info daily}}
  	{{/each}}
{{else}}
    <h6 class="pb-4 has-text-centered is-italic">You do not have any trips yet</h6>
    <a href="/api/trip-details/{{trip.id}}"><button id="add-daily-btn" class="button is-warning is-pulled-right">add daily notes</button></a>


{{/if}} --}}