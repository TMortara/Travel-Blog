{{#each dailys as |daily|}}
    {{> trip-info trip}}
      <a href="/api/trip-details/{{trip.id}}"><button id="edit-trip-btn" class="button is-warning is-pulled-right">edit trip</button></a>
  	{{/each}}
{{else}}
<h6 class="pb-4 has-text-centered is-italic">You do not have any trips yet</h6>

{{/if}}