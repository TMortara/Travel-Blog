<div class="each_trip">
    <h2 class="each_trip_title">{{title}}</h2>
    <p class="each_trip_body">{{trip_content}}</p>
    <div class = "align_right">
        <!--
            Note: "format_date" is pulled from /utils/helpser.js
        -->
        <P>Posted By: {{user.username}} | {{format_date created_at}}</P>
    </div>
    